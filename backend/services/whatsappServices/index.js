const sendTextMessage = require("./sendTextMessage");
const sendInteractiveButtons = require("./sendInteractiveButtons");
// const sendTemplateMessage = require("./sendTemplateMessage"); // Add if needed later

module.exports = {
  sendTextMessage,
  sendInteractiveButtons,
};
